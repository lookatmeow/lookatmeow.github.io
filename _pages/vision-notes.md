---
title: Vision Notes
permalink: /vision-notes/
---

## Vision Project Notes

- Keep homepage at `/` as the primary product experience.
- Keep long-form docs and updates in `_pages` and `_posts`.
- Use `assets`, `images`, and `files` for static project resources.
